#include <stdio.h>
/**
 * main - a program that prints single digits
 *
 * Return: 0 complete
 */
int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
