#include <stdio.h>
/**
 * main -write a program that uses printf to display output
 * Return: 0 (completed)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
